# Example Environment Configuration
# Copy this file to .env and update with your values
# Then source it before running the backup: source .env

# Required variables
export SSH_USER="your-username"
export SSH_HOST="your-server.com"
export SSH_PATH="/var/www/html/wordpress"

# Optional variables
# export SSH_PUBLIC_KEY="$(cat ~/.ssh/id_rsa)"
# export SSH_MYSQL_PATH="/usr/bin/mysql"

# Backup configuration
# Comma-separated list of folders within wp-content to backup
# Default: uploads,languages
export backup_include_only_folders="uploads,languages"

# Usage:
# 1. Copy this file: cp env.example .env
# 2. Edit .env with your actual values
# 3. Source the environment: source .env
# 4. Run backup: ./backup.sh